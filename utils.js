const sum = (b, c) => {
    return b + c
}

const substrct = (b, c) => {
    return b - c
}

const product = (b, c) => {
    return b * c
}

const divide = (b, c) => {
    return b / c
}

module.exports = {
    sum,
    substrct,
    product,
    divide
}